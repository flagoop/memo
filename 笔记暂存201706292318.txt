全局变量：在多个源文件中，全局变量可以互相调用 ，但要加上说明符 如extern
static全局变量：在多个源文件中，此类变量只能存在于当前源文件中，不能被其它源文件调用

static局部变量：首先它只存在于当前源文件中，然后有一个特点 ，在此类变量当前的函数调用结束后，其分配的地址和内存块是不会跟着函数消失的，下次调用这个函数的时候还可以用到这个
		static局部变量的值，而且是最后一次用到的值 。当然 是要调用这个函数的时候。

static变量在内存中在某个固定区域内连续存放的，而普通变量是离散存放的。